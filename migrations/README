# Как добавить новую колонку в любую в любую из таблиц БД:

1. Добавить колонку в таблицу User или Role в файле db.py
2. В консоли напишите команду `alembic revision --autogenerate -m "comment"`
3. В консоли напишите команду `alembic upgrade head`